@model IEnumerable<prjPetAdoption.Models.board>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.boardTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.boardContent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.animalData.animalKind)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUsers.Email)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.boardTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.boardContent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.animalData.animalKind)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUsers.Email)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.boardID }) |
            @Html.ActionLink("Details", "Details", new { id=item.boardID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.boardID })
        </td>
    </tr>
}

</table>
